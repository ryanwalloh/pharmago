[phases.setup]
nixPkgs = ["python311", "postgresql_16", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT pharmago.asgi:application"

